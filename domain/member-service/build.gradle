dependencies {
    api project(":domain:rds:rds-member")
    testApi project(":domain:rds:rds-member").sourceSets.test.output
    api project(":domain:redis:redis-member")
    testApi project(":domain:redis:redis-member").sourceSets.test.output

    implementation("org.springframework.boot:spring-boot-starter-mail")
}

bootJar.enabled(false)
jar.enabled(true)