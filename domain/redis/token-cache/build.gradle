dependencies {
    api project(":domain-redis-common")
    testApi project(":domain-redis-common").sourceSets.test.output

    api("org.springframework.boot:spring-boot-starter-security")
}

bootJar.enabled(false)
jar.enabled(true)